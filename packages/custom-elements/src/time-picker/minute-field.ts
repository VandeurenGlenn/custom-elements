import { customElement } from 'custom-element-decorator';
import { LitElement, html, css } from 'lit';
import { property } from 'lit/decorators.js'
// import 

@customElement()
export class TimePickerMinuteField extends LitElement {
  static styles = [
    css`
      :host {
        display: block;
      }
    `
  ];

  render() {
    return html``;
  }
}
