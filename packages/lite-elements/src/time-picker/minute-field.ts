import { customElement, LiteElement, css, html } from '@vandeurenglenn/lite'
// import

@customElement('custom-time-picker-minute-field')
export class TimePickerMinuteField extends LiteElement {
  static styles = [
    css`
      :host {
        display: block;
      }
    `
  ]

  render() {
    return html``
  }
}
